include:
  - path: ../docker-compose.yml

# env_file paths are relative to this directory (deploy/production/).
# Place .env here: deploy/production/.env
services:
  vendure-server:
    env_file: .env
  vendure-worker:
    env_file: .env
  storefront:
    env_file: .env
